name: Playwright Tests

permissions:
  contents: read
  pages: write

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests (all)
        run: |
          npx playwright test tests --reporter=list --reporter=allure-playwright --reporter=html
        env:
          CI: true

      - name: List current directory
        run: ls -la

      - name: List allure-results directory
        run: ls -la allure-results || echo "No allure-results directory!"

      - name: Generate Allure Report
        if: always()
        run: npx allure generate allure-results --clean -o allure-report || true

      - name: List allure-report directory
        run: ls -la allure-report || echo "No allure-report directory!"

      - name: Package reports for Pages
        if: always()
        run: |
          rm -rf report-site || true
          mkdir -p report-site/full/allure report-site/full/playwright
          if [ -d "allure-report" ]; then cp -r allure-report/* report-site/full/allure/ || true; fi
          if [ -d "playwright-report" ]; then cp -r playwright-report/* report-site/full/playwright/ || true; fi

      - name: Upload reports artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: combined-reports
          path: report-site/
          retention-days: 30

      - name: Deploy reports to gh-pages (merge into reports/full)
        if: always()
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -e
          REPO="${{ github.repository }}"
          BRANCH=gh-pages
          TARGET_DIR=reports/full
          # clone gh-pages (or create it) then update subfolder
          if git ls-remote --exit-code origin ${BRANCH}; then
            git clone --depth 1 --branch ${BRANCH} https://x-access-token:${GITHUB_TOKEN}@github.com/${REPO} gh-pages
          else
            git clone https://github.com/${REPO} gh-pages || true
            mkdir -p gh-pages
            cd gh-pages
            git checkout --orphan ${BRANCH} || true
            cd ..
          fi
          rm -rf gh-pages/${TARGET_DIR} || true
          mkdir -p gh-pages/${TARGET_DIR}
          cp -r report-site/full/* gh-pages/${TARGET_DIR}/ || true
          cd gh-pages
          git add ${TARGET_DIR}
          git commit -m "chore(reports): update full reports from ${GITHUB_REF}" || echo "no changes to commit"
          git push https://x-access-token:${GITHUB_TOKEN}@github.com/${REPO} ${BRANCH}

      - name: Upload Playwright Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
